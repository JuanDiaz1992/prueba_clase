const traerDatos =()=>{
    fetch("url",{

    })
    .then(response=>response.json())
    .then(data=>{
        let horas = data.data;
        for (let index = 0; index < horas.length; index++) {
            const element = array[index];
            
        }
    })
}